# The Manager's open — run `r-2026-09-02-0001`

Landed on disk before any seat runs, per my own arm-2a ruling at the Step 2 Wave 2 open. This is that
disk-write.

The question asked, four days after `r-2026-08-29-0001` asked it byte-identically modulo a line
break, is the special-cause temptation of this run: treat it as a new question because it arrived in
a new console turn. It is not a new question. Every check below is written to find out whether that
holds, not to assume it.

---

## 1. The four preconditions of `client_note_prompts.md` §1

**1. The annex exists and is complete under `deliverable_shape.md`.** Checked the annex,
`cr_scratch/postmortem_deloitte_evidence/lunar-economy-valuation-review.md`, against
`deliverable_shape.md` §1's closed heading set. All five present, in order, under the exact headings:
`## Question`, `## Verdict`, `## What was tested, and how it could have failed`, `## Sources`,
`## What remains unverified`. §3 carries 35 tested rows across seven groups. §5 states its own
denominator: "Nine items unverified, over 35 claims and 35 tests examined." **Holds.**

**2. The question the note answers is the question that was asked.** This is the one that can fail on
a reused annex, so I read the annex's `## Question` against the console text in this run's brief
rather than against the run id.

Annex: *"in the downloads folder there is a file named deloitte-building-the-lunar-economy-report.pdf.
did they get it right? what did they miss? thanks"*
This run: *"in the downloads folder there is a file named deloitte-building-the-lunar-economy-report.pdf.
did they get it right? what did they miss?\nthanks"*

Identical modulo the line break, and it names the same file at the same path. Text identity alone
would not be enough — a reader could rename a different PDF into that path — so I weigh it against
three independent facts, not one: (a) both working copies sit at the same verified refs this annex was
built against (`cr-agents f0c976b`, `lsei 7f97983` — confirmed live: `git -C cr-agents rev-parse
--short HEAD` returns `f0c976b`, `git -C lsei rev-parse --short HEAD` returns `7f97983`, both matching
the last row naming each copy in `oracle/VERIFIED.tsv`); (b) the corpus this session bootstrapped is
the same corpus the annex traces into — no shelf move, no re-taxonomy; (c) the orchestrator's
verification pass this session re-extracted the PDF at that path with a fresh `pdftotext -layout` and
reproduced all ten Group 0 measurements exactly — ore-grade 0, excavation hits 3 with no rate, discount
count 2 at the same two locations, self-replication 0, the `$343B–$566B` and `$114.5B` figures present,
the `$20 million per kilogram` helium-3 figure present, Jones 2019 at endnote 170 with neither
headline figure in the body. Ten independent string- and count-level measurements reproducing exactly
is stronger evidence of file identity than a hash would be, because a hash match wouldn't tell me the
content is still the content the annex tested — this does.

**Ruling: the annex may be reused.** No new evidence pass, no new §3 procedure. What is *not* covered
by this ruling: the annex's §5 item 1 (discount-rate arithmetic owed a `findings/` entry) and item 9
(corpus fork `unknown`) are still open, on this install, for this run, exactly as they were four days
ago — reuse does not close them, it inherits them, and the note's Methods paragraph should say so
without dating them to the earlier run. That is a Methods fact, not process narrative: it bears on
what the conclusions rest on.

**3. `cr-agents/` is present at a known ref.** `git -C cr-agents rev-parse --short HEAD` → `f0c976b`,
matching `oracle/VERIFIED.tsv`'s sole `cr-agents` row. **Holds**, checked live, not carried from the
brief.

**4. The audience and the length are fixed.** Audience: default, per `client_note_prompts.md` §2, with
the same one-clause override the annex's own subject makes natural — see §2 below. Length: set at this
open in §4 below, as a test-plan requirement, not left to drafting judgement. **Fixed by this
document.**

All four hold. The sequence opens.

---

## 2. The six slots

`client_note_prompts.md` §9 is a worked proposal for this exact annex, written by another seat. I take
four of six as proposed, amend one, and hold the sixth to a stronger standard than proposed.

| Slot | Fill | Disposition |
|---|---|---|
| `OBJECT` | a technical note for publication | Taken as proposed. |
| `SUBJECT` | the valuation method in a consultancy estimate of the 2026–2050 lunar economy | Taken as proposed. |
| `PURPOSE` | The estimate is transparent about three of its own weaknesses — its reliance on terrestrial learning-rate proxies, the unresolved ice concentration behind its resource pool, and the non-additive treatment of its largest single value pool — but its headline range rests on inputs it never states: an ore grade, an excavation rate, and a reference class, plus a discount rate its own Limitations section omits despite conceding the value is end-loaded. Establish which of its conclusions survive that combination, and which of its stated limitations are the real ones. | **Amended.** The proposed fill in §9 answers only "what did they miss." The author asked two questions, and a `PURPOSE` that drops the first produces a hit piece on a report the annex itself credits three times (Group 0, rows 8, 9 and 10). This fill states both halves and names the credit rows, not just the gap rows, so the outline gate at prompt 2 has something to hold the Writer to on the "got it right" side. |
| `AUDIENCE` | Default: a cognizant reader in a space-adjacent role who has read the estimate and not its sources | Taken as proposed. The override on the base default (§2's "has not read the sources" reader is here also a reader of the report itself) is correct for a review of a named document and I would have written the same override independently. |
| `LENGTH` | 2,500–4,000 words, all seven sections — "a review of a document, position or method," `client_note.md` §3.1 | Taken as proposed, on my own independent reasoning: see §4 below. Not a rubber stamp — I checked it against the alternative tier before accepting it. |
| `EXCLUSIONS` | §4 verbatim, plus: do not name the six register axes (disagreements described in words or not at all); plus, added here: **do not date the underlying review, and do not state or imply that this note answers a question asked twice** — the reuse of a four-day-old annex is a fact about this project's process and belongs in no sentence of the note. | **Amended** with one addition specific to this run's risk, not present in the §9 proposal because that proposal was written for the first asking. |

---

## 3. Evidence-pass line, `answer_contract.md` §10 fixed form

This run's own concurrent tool pass and the annex's inherited one are different objects and I am not
folding them into one line that would misstate which did the work.

**This run's pass.** `C.adviseQuestion` scored this run's raw question string and returned 5 findings,
all retrieval candidates, 3 confirmed at threshold 0.28, top-scored
`space-law-and-governance/hague-working-group-2019-building-blocks-space-resources.md`, against a
question whose tokens are a filename and a plain-English ask.

*Evidence pass: took none of the 5. Set aside all 5 (the retrieval candidates scored by
`C.adviseQuestion` this run, top `hague-working-group-2019-building-blocks-space-resources.md`): the
grammar this tool scores is the question string, and this question string names a file and asks two
plain questions about it — no sub-claim in it addresses space law, governance or a 2019 Hague working
group. I read the question and the axis and the tool did not, per `answer_contract.md` §10; that
sentence is sufficient and I am using it once, for all five.*

**The inherited pass.** This run reuses the `r-2026-08-29-0001` annex under the §1 ruling above, and
the operative evidence pass is that annex's own, unchanged: took `LCC-12`, `LCC-11`, `LCC-13`, `LCC-15`;
set aside `LCC-06`, `LCC-09`, `LCC-10` (tied at mass 0.428, the tool's documented indistinguishability
case, read as background rather than as claims about beneficiation, illumination or reactor-versus-solar
mass); added `ECR-15`, `ECR-16` against the tool, on the same §10 licence, for the same stated reason.
I re-examined this pass rather than accepting it uninspected — the six axes it selects are exactly the
ones the annex's Group 0 measurements and Groups 1 through 6 test, and nothing in this run's fresh
extraction contradicts the selection — and I let it stand.

---

## 4. Length tier

**2,500–4,000 words, all seven sections: Abstract, Introduction, Background, Methods, Results,
Discussion, Conclusions.** No section dropped. This is the "review of a document, position or method"
tier of `client_note.md` §3.1, and I chose it independently rather than accepting the §9 proposal on
its say-so: the question is not settled by one figure (ruling out the 400–800 tier) and does not turn
on one contested input (ruling out the 1,200–2,000 tier) — it spans seven distinct measured claims
(ore grade, excavation rate, discount rate, reference class, price destination, helium-3 demand,
self-replication) plus three credited strengths, which is a review of a document's method, not a
single-input dispute.

This is a test-plan requirement, not a drafting suggestion: the Software Engineer's test plan carries
a word-count band and a section-presence check as whole-document tests, alongside the mandatory W1–W8
block, and a draft outside 2,500–4,000 words or missing a section fails before it reaches The Editor.

---

## 5. Seats, order, and what I expect to go wrong

| Order | Seat | Act | Gate after |
|---|---|---|---|
| 1 | The Software Engineer | Prompt 1: test suite over the annex, per §6 below | Human approves the test plan |
| 2 | The Writer | Prompt 2: paragraph-level outline, topic sentences bracketed to annex rows | I validate the outline against the test plan; human approves |
| 3 | The Writer | Prompt 3: draft to the outline; strips brackets into `cr_scratch/r-2026-09-02-0001_note_map.tsv` | — |
| 4 | The Fact-Checker | Gates the map: every note claim to an annex row, no claim without one | Must pass before prompt 4 opens; a note is not edited for prose while it still carries an unmapped claim |
| 5 | The Editor | Prompt 4: audits against `signs_of_ai_writing.md` and the eight banned constructions; re-runs whole-document tests after revision | Human approves; note ships |
| 6 | The Manager | Closes: judges whether the note ships | — |

**What I want from The Software Engineer at prompt 1 that a generic test-plan request would not get.**
A generic request, run mechanically over the annex's 35 rows, will produce a test plan weighted toward
the gap rows because there are more of them and they are more mechanically testable (a grep for an
absent term is easier to spec than a test for whether a credit was represented proportionately). I am
requiring, by name, at least three section-level tests drawn from Group 0 rows 8, 9 and 10 — the
terrestrial-proxies credit, the ice-concentration credit, and the non-additive Value Pool 3 credit —
each naming which section carries it, so the "did they get it right" half of `PURPOSE` has enforced
addresses in the plan rather than living only in my prose above. I am also requiring a Results-section
test for the cross-source discount-rate arithmetic (41.7× and 1.94× present-value ratios, Group 1 last
row): under `client_note.md` §6 this is the one place a signed note may compute across annex rows
rather than cite one, and the test plan should require the computation shown, not just the two input
rates cited separately.

**The one thing I expect to go wrong.** Not a repeat of the postmortem's failure — a sub-agent runs
this time, the sequence is disk-first. What I expect is narrower and specific to a reused annex: the
annex is unsigned, non-adjudicating, and structured as six contested axes with credits folded in as
three rows out of thirty-six. A seat reading it under time pressure reproduces that shape's balance by
default — heavy on the gaps, light on the credits — because the annex itself reads that way on the
page even though its content supports both halves. The amended `PURPOSE` and the three named
section-level tests above are the control for exactly this, and if the outline at prompt 2 does not
show the credits carried forward as visibly as the gaps, that is a system problem with this run's
briefing, not a one-off lapse by the Writer, and it routes back to this open rather than to a
prompt-3 correction.
